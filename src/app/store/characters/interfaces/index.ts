export enum CharactersStatus {
  Default = '[CHARACTERS] Characters are default',
  Load = '[CHARACTERS] Characters are loading',
  Failure = '[CHARACTERS] Load is failure',
  Success = '[CHARACTERS] Characters loaded successfully',
}

export enum CharacterStatus {
  Default = '[CHARACTER] Character are default',
  Load = '[CHARACTER] Character are loading',
  Failure = '[CHARACTER] Load is failure',
  Success = '[CHARACTER] Character loaded successfully',
}
